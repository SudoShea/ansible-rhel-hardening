# ==============================================================================
# File        : roles/cis_hardening/templates/audit.rules.j2
# Description : CIS compliant ruleset for auditd execution tracking
# Author      : SudoShea
# Version     : 1.1.0
# License     : MIT
# ==============================================================================

# First rule - delete all existing rules
-D

# Set buffer size
-b 8192

# Failure mode: 1 = printk, 2 = panic
-f 1

# Record changes to system time
-a always,exit -F arch=b64 -S adjtimex -S settimeofday -k time-change
-a always,exit -F arch=b64 -S clock_settime -k time-change

# Record changes to identity and group files
-w /etc/group -p wa -k identity
-w /etc/passwd -p wa -k identity
-w /etc/gshadow -p wa -k identity
-w /etc/shadow -p wa -k identity
-w /etc/security/opasswd -p wa -k identity

# Record network environment changes
-w /etc/issue -p wa -k system-locale
-w /etc/issue.net -p wa -k system-locale
-w /etc/hosts -p wa -k system-locale
-w /etc/hostname -p wa -k system-locale

# Track privilege execution and login events
-w /var/log/faillog -p wa -k logins
-w /var/log/lastlog -p wa -k logins
-w /var/log/tallylog -p wa -k logins

{% if cis_audit_immutable | default(false) %}
# Make audit configuration immutable (requires reboot to modify)
-e 2
{% else %}
# Immutability (-e 2) disabled for homelab configuration flexibility
{% endif %}
